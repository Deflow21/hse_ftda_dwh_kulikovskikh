# TYPE  DATABASE        USER            ADDRESS                 METHOD

# Разрешить локальные подключения (через Unix-сокет)
local   all             all                                     trust

# Разрешить подключения по TCP/IP со всех адресов с использованием пароля
host    all             all             0.0.0.0/0               md5

# Пример ограниченного доступа (раскомментируйте и настройте при необходимости)
# host    all             all             192.168.1.0/24          md5